---
kubernetes_user: kubeuser
##
kubernetes_user_pass: kuberCucumber
##
kubernetes_network: kubectl apply -f "https://cloud.weave.works/k8s/net?k8s-version=$(kubectl version | base64 | tr -d '\n')"
##
kubernetes_version: 1.23.0
